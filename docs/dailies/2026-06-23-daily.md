# Daily - 2026-06-23

## Informacion general

| Campo | Valor |
|---|---|
| Fecha | 2026-06-23 |
| Sprint | Sprint 2 - Cierre de Nivel Medio |
| Rama de trabajo | `feature/feedback-monitoring` |

## Objetivo del dia

Completar los requisitos del Nivel Medio, integrar el trabajo de optimizacion de hiperparametros y dejar la aplicacion Streamlit preparada para feedback, monitorizacion y recogida de datos nuevos para futuros reentrenamientos.

## Trabajo realizado

- Se integro en la rama de feedback la version actual de `dev`.
- Se reviso y ejecuto `notebooks/04_hyperparameter_optimization.ipynb`.
- Se anadio `optuna` a `requirements.txt`.
- Se corrigio la conclusion del notebook de optimizacion para no prometer exportacion de modelo si el baseline sigue ganando.
- Se confirmo que el baseline sigue siendo el mejor modelo global frente a los modelos optimizados evaluados.
- Se renombro la validacion cruzada a `notebooks/05_cross_validation.ipynb` para mantener la secuencia despues de optimizacion.
- Se mejoro la monitorizacion de feedback en Streamlit.
- Se anadio gestion de registros de feedback: completar valor real, editar valor real y eliminar registros con confirmacion.
- Se anadio un identificador `prediction_id` para trazabilidad de predicciones.
- Se sincronizo el feedback con `data/new_data/nuevos_registros.csv`.
- Se implemento la vista `Pipeline de reentrenamiento`.
- Se anadio generacion local de `data/processed/retraining_dataset.csv` con registros validados.
- Se actualizo la documentacion de la app Streamlit.
- Se actualizo el README con el estado del Nivel Medio.

## Resultados de optimizacion

| Modelo | Metodo | RMSE test | MAE test | R2 test | Overfitting R2 |
|---|---|---:|---:|---:|---:|
| Linear Regression | Baseline | 0.0201 | 0.0158 | 0.8449 | 0.077% |
| LightGBM | Optuna | 0.0208 | 0.0164 | 0.8340 | 1.33% |
| XGBoost | RandomizedSearchCV | 0.0210 | 0.0165 | 0.8311 | 1.15% |
| LightGBM | GridSearchCV | 0.0213 | 0.0168 | 0.8258 | 3.82% |
| XGBoost | Optuna | 0.0215 | 0.0173 | 0.8215 | 5.47% |

Conclusion: el baseline sigue siendo el mejor modelo global. No se exporta un modelo optimizado porque no mejora el RMSE del baseline.

## Estado de Nivel Medio

| Requisito | Estado | Evidencia |
|---|---|---|
| Modelo con tecnicas ensemble | Hecho | `notebooks/03_ensemble-techniques.ipynb` |
| Validacion cruzada | Hecho | `notebooks/05_cross_validation.ipynb` |
| Optimizacion de hiperparametros | Hecho | `notebooks/04_hyperparameter_optimization.ipynb` |
| Feedback para monitorizar performance | Hecho | Vista `Monitorizacion` en `app/app.py` |
| Recogida de datos nuevos para reentrenamiento | Hecho | Vista `Pipeline de reentrenamiento` en `app/app.py` |

## Decisiones tomadas

- Mantener `Linear Regression Baseline` como modelo productivo mientras no exista un modelo optimizado que mejore sus metricas.
- Usar `prediction_id` para relacionar feedback y registros nuevos.
- Tratar los registros sin valor real como `pending_target`.
- Tratar los registros con valor real como `validated_for_retraining`.
- Generar el dataset de reentrenamiento solo con registros que tengan valor real observado.
- No subir CSV locales generados por la app.

## Pruebas realizadas

- Ejecucion completa de `notebooks/04_hyperparameter_optimization.ipynb` con `jupyter nbconvert --execute`.
- Validacion de notebooks con `nbformat`.
- Comprobacion de sintaxis con `python -m py_compile app/app.py app/paths.py`.
- Arranque local de Streamlit en `http://localhost:8501`.
- Revision de caracteres raros en `app/app.py`.

## Riesgos o pendientes

- Revisar visualmente la app con el equipo antes de mergear definitivamente a `dev`.
- Confirmar en la Pull Request que los CSV generados localmente no se suben.
- Si se avanza a Nivel Avanzado, valorar migrar los CSV a una base de datos como SQLite o MongoDB.
