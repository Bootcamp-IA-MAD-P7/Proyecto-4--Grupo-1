# Daily - 2026-06-26

## Informacion general

| Campo | Valor |
|---|---|
| Fecha | 2026-06-26 |
| Sprint | Sprint 2 - Nivel Avanzado |
| Rama de trabajo | `feature/docker-aws-deployment` |

## Objetivo del dia

Avanzar en los requisitos de Nivel Avanzado pendientes: dockerizacion de la aplicacion, inclusion de tests unitarios y preparacion del despliegue en AWS.

## Trabajo realizado

- Se creo `Dockerfile` para ejecutar la app Streamlit en un contenedor.
- Se creo `.dockerignore` para excluir datos locales, secretos, entornos virtuales y archivos generados.
- Se creo `requirements-docker.txt` con dependencias reducidas para ejecutar la app dentro de Docker.
- Se construyo la imagen Docker `flood-risk-app`.
- Se ejecuto el contenedor exponiendo el puerto `8501`.
- Se valido la app en `http://localhost:8501` desde el contenedor.
- Se creo `requirements-dev.txt` para dependencias de desarrollo.
- Se anadio `src/pipeline.py` para aislar la logica de construccion del dataset de reentrenamiento y poder testearla sin cargar la UI.
- Se anadio carpeta `tests/` con pruebas unitarias de feature engineering, base de datos SQLite y pipeline de reentrenamiento.
- Se actualizo el README con instrucciones de Docker y tests.

## Estado de Nivel Avanzado

| Requisito | Estado | Evidencia |
|---|---|---|
| Version dockerizada del programa | Hecho | `Dockerfile`, `.dockerignore`, `requirements-docker.txt` |
| Guardado en base de datos | Hecho | `src/database.py` y SQLite local |
| Inclusion de tests unitarios | Hecho | `tests/` |
| Despliegue | Pendiente | Siguiente paso: AWS |

## Tests anadidos

| Test | Objetivo |
|---|---|
| `tests/test_features.py` | Validar indicadores de feature engineering |
| `tests/test_database.py` | Validar ciclo de guardado, actualizacion y borrado en SQLite |
| `tests/test_pipeline.py` | Validar que el dataset de reentrenamiento solo usa registros con valor real |

## Pruebas realizadas

```bash
pip install -r requirements-dev.txt
python -m pytest
```

Resultado:

```text
5 passed
```

Tambien se valido Docker:

```bash
docker build -t flood-risk-app .
docker run --rm -p 8501:8501 flood-risk-app
```

La app cargo correctamente en:

```text
http://localhost:8501
```

## Decisiones tomadas

- Mantener Docker como requisito avanzado completado localmente.
- Usar una lista de dependencias especifica para Docker para evitar paquetes innecesarios de notebooks o Windows.
- Testear la logica critica fuera de Streamlit para que los tests sean rapidos y estables.
- Preparar AWS como siguiente fase de despliegue.
- Valorar AWS App Runner como opcion principal para desplegar la imagen Docker.

## Pendientes

- Desplegar la app en AWS.
- Documentar el despliegue final cuando este validado.
- Actualizar JIRA con Docker, tests y despliegue.
