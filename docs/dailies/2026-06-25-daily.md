# Daily - 2026-06-25

## Informacion general

| Campo | Valor |
|---|---|
| Fecha | 2026-06-25 |
| Sprint | Sprint 2 - Cierre funcional, documentacion y preparacion de despliegue |
| Rama de trabajo | `feature/database-pipeline-feature-engineering` |

## Objetivo del dia

Cerrar la integracion de base de datos, mejorar la aplicacion Streamlit a nivel funcional y visual, integrar feature engineering, reforzar el pipeline de ingesta y acordar los siguientes pasos: Docker, despliegue, presentaciones y documentacion final.

## Trabajo realizado

- Se reviso la rama de base de datos y se detecto que la integracion original no estaba conectada funcionalmente con la app.
- Se sustituyo la dependencia practica de PostgreSQL por una persistencia local en SQLite para facilitar ejecucion y defensa del proyecto.
- Se creo `src/database.py` para inicializar la base, guardar predicciones, actualizar valores reales, eliminar registros y consultar resumen.
- Se conecto la app Streamlit con SQLite desde las vistas de prediccion, monitorizacion y base de datos.
- Se elimino el archivo real de secretos `app/.streamlit/secrets.toml` y se sustituyo por `app/.streamlit/secrets.example.toml`.
- Se actualizo `.gitignore` para impedir la subida de secretos, bases SQLite y archivos generados por la app.
- Se creo `src/features.py` con una capa de feature engineering basada en indicadores compuestos de riesgo.
- Se integro feature engineering como lectura explicativa en la vista `Prediccion`.
- Se integro feature engineering en el dataset generado por el pipeline de ingesta.
- Se mejoro la vista `Pipeline de reentrenamiento` para mostrar claramente cuando se activa el boton `Ejecutar pipeline de ingesta`.
- Se aclaro que el boton del pipeline no reentrena el modelo activo: prepara datos validados para un reentrenamiento posterior.
- Se incorporo una vista `Base de datos` para comprobar persistencia estructurada en SQLite.
- Se integro PyGWalker como explorador visual del dataset dentro de la vista `Datos`.
- Se mejoro la narrativa interna de la app dentro de la propia interfaz, especialmente en `Guia del proyecto`.
- Se mejoro la estetica general de la app con fondo meteorologico, cabecera visual, tarjetas, contraste, tablas y controles.
- Se corrigieron problemas visuales de contraste en botones, desplegables, tooltips, tablas y notebooks renderizados.
- Se encapsulo el arranque de Streamlit en `main()` para mejorar mantenibilidad.
- Se revisaron imports, rutas, checks de dependencias, compilacion y carga local de la app.
- Se actualizo el README como documento principal del proyecto.
- Se incorporo en README la distribucion de tareas por participante.
- Se preparo un documento narrativo interno para el equipo, fuera de Git, con el flujo de defensa de la app.
- Se acordo que la documentacion oficial debe concentrarse principalmente en el README para evitar dispersion.

## Estado funcional de la app

| Vista | Estado | Resultado |
|---|---|---|
| Guia del proyecto | Hecho | Explica que hace la app, como usarla, como interpretar resultados y como funciona el pipeline |
| Prediccion | Hecho | Calcula riesgo, guarda feedback, muestra feature engineering y persiste en SQLite |
| Monitorizacion | Hecho | Permite completar valor real, editar registros, eliminar registros y calcular metricas |
| Base de datos | Hecho | Muestra estado de SQLite, predicciones guardadas y registros recientes |
| Pipeline de reentrenamiento | Hecho | Activa el boton de ingesta cuando existen registros con valor real |
| Informes tecnicos | Hecho | Muestra notebooks con contexto |
| Datos | Hecho | Muestra tabla y explorador visual integrado con PyGWalker |

## Decisiones tomadas

- Usar SQLite como base de datos funcional local para la app.
- Mantener los CSV como respaldo y como artefactos generados por la aplicacion.
- No subir archivos generados localmente: feedback, nuevos datos, datasets procesados ni bases SQLite.
- Mantener el modelo baseline actual con las 20 variables originales para no romper compatibilidad con el modelo entrenado.
- Preparar feature engineering para el dataset de reentrenamiento y futuros modelos.
- Dejar el documento narrativo interno fuera de Git mediante `docs/internal/`.
- Concentrar la documentacion oficial en `README.md`.
- Mantener `docs/dailies/` como registro de seguimiento del trabajo diario.
- Dejar Dockerizacion y despliegue como siguiente bloque de trabajo.
- Preparar una presentacion final apoyada en el flujo de la app: prediccion, feedback, base de datos, pipeline y feature engineering.

## Distribucion de tareas acordada

| Participante | Bloque principal | Responsabilidades |
|---|---|---|
| Participante 1 | Analisis exploratorio y datos | Dataset, revision de calidad, distribuciones, correlaciones, visualizaciones y explicacion del EDA |
| Participante 2 | Modelado baseline y metricas | Entrenamiento inicial, evaluacion, overfitting, residuos, prediccion vs real y guardado del modelo |
| Participante 3 | Mejora y validacion | Ensemble techniques, validacion cruzada, optimizacion de hiperparametros y comparacion frente al baseline |
| Participante 4 | Productivizacion y cierre | App Streamlit, feedback, base de datos, pipeline de ingesta, feature engineering, documentacion, preparacion de Docker/despliegue y presentacion |

## Acuerdos para siguientes pasos

- Preparar Dockerizacion de la app.
- Preparar despliegue.
- Preparar presentacion final del proyecto.
- Revisar la narrativa de defensa para que el flujo de la aplicacion se entienda sin entrar en exceso tecnico.
- Mantener README como documento principal y evitar duplicar informacion en varios `.md`.

## Pruebas realizadas

```bash
python -m py_compile app\app.py app\paths.py src\features.py src\database.py src\db_insert.py
python -m pip check
git diff --check
```

Tambien se comprobo la carga local de Streamlit en:

```text
http://localhost:8501
```

## Riesgos o pendientes

- No existe una carpeta formal de tests unitarios; se recomienda anadir tests minimos para `src/features.py`, `src/database.py` y construccion del dataset de reentrenamiento.
- La app esta preparada para ejecucion local. Dockerizacion y despliegue quedan acordados como siguiente fase.
- Antes del merge final conviene revisar visualmente la app en el equipo y confirmar que no se suben archivos generados localmente.
